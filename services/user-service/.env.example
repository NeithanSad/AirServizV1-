# user-service environment variables
# Copy to .env and fill in values for local development

PORT=3005

# PostgreSQL — shares users_db with auth-service
DB_HOST=postgres-users
DB_PORT=5432
DB_NAME=users_db
DB_USER=users_admin
DB_PASS=change_me
